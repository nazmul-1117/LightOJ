
//Unsolved Problem

#include <bits/stdc++.h>
#define lld long long int

using namespace std;

int32_t main(){
    ios_base :: sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int16_t T;

    int n;
    cin >> n;

    for (int i = 1; i <= n; ++i){
    	if (49%i == 0){
    			cout << i << " ";
    		}
    }

  
}